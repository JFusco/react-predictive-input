jest.disableAutomock();

import React from 'react';
import Suggestion from '../src/component/js/Suggestion';

describe('Suggestion', () => {
	it('should render', () => {
		expect(2).toBe(2);
	});
});
